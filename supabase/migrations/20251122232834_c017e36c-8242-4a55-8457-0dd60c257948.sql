-- Drop content_templates table (with CASCADE to handle dependencies)
DROP TABLE IF EXISTS public.content_templates CASCADE;

-- Drop ai_models table (with CASCADE to handle dependencies)
DROP TABLE IF EXISTS public.ai_models CASCADE;